/// <reference types="cypress" />
import loader from '../../../support/loader'

describe('Cart payplug spec', () => {

	loader.loadPO('frontend/landingPage')
	loader.loadData('lang')

	it( 'Cart Oney animation disabled', function(){});

	it( 'Cart Oney animation enabled', function(){});


});
